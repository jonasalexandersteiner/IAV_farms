

#summary statistics 

summarytools::descr(df2)


table1(~ production_type + herdsize_cat + herdsize + SIV_percentage,
       data = df1)

#Missing values

colSums(is.na(df2))

#group-wise summaries 

df1 %>%
  group_by(herdsize_cat) %>%
  summarize(
    Mean_SIV = mean(SIV_percentage, na.rm = TRUE),
    SD_Weight = sd(SIV_percentage, na.rm = TRUE),
    Count = n()
  )

#creating frequency and proportion table

table(df1$herdsize_cat, df1$herdsize_cat)

prop.table(table(df1$herdsize_cat, df1$SIV), margin = 2)

table1(~ herdsize_cat | SIV, data=df1)


#histogramm 

hist(df1$herdsize)

#boxplot 

boxplot(
  herdsize ~ SIV,
  data = df1,
  main = "Herdsize by SIV status",
  xlab = "SIV",
  ylab = "Herdsize",
  col = c("lightblue", "lightgreen", "lightyellow") 
)


# bar chart 

# Bar chart 
barplot(table(df1$production_type),
        main = "Production types of pig farms",
        xlab = "Prodcution type",
        ylab = "number",
)

# scatterplot


# Scatterplot
plot(
  df1$herdsize, df1$SIV_percentage,
  main = "Scatterplot Herdsize vs. SIV_percentage",
  xlab = "Herdsize (number of pigs)",
  ylab = "Percentage of SIV positive pigs (%)",
  col = "darkgreen",
  pch = 3 # with this number you can specify the shape of the points
)